
<table class="table table-stripped">
<tr>
<th> Student Name </th>
<th> Links</th>
</tr>
<% @parent.users.each do |i| %>
<tr>
<td><%= i.first_name %></td>
<td><%= link_to "View Attendance", attendance_report_parent_dashboard_path(i) , :class => "btn btn info"%>
<%= link_to "Mark reports", marks_parent_dashboard_path(i), :class => "btn btn info" %></td>
</tr>
<% end %>
</table>