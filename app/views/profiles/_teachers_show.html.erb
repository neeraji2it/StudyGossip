          <% attendance = TeacherAttendance.where(:user_id=>current_user.id,:created_at=>Date.today.beginning_of_day..Date.today.end_of_day) %>

          <% if attendance.present?   %>
          Attendance Given |
          <% else %>

          <%= link_to "Attendence", new_teacher_attendence_path(:user_id => current_user.id),:class=>"btn btn-mini btn-success", :remote => true%> 
          <% end %>
          <%= link_to "Record", teacher_attendences_path(:user_id => current_user.id),:class=>"btn btn-mini btn-success",:remote=>true%> 