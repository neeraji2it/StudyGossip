<%= form_for @teacher_attendence, :remote => true,:url=>teacher_attendences_path, :method => "POST", :html=> { :class=> "form-horizontal", :style=> "display:inline;" } do |f| %> 
  <%= render :partial=>'form',:locals => {:f=>f} %>
<% end %>